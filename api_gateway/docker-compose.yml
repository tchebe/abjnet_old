version: '3.5'

services:
  abjnet_api_gateway:
    container_name: abjnet_api_gateway
    restart: always
    image: microhq/micro:latest
    # build: .
    command: api --namespace=abjnet --handler=http
    # ports:
    #   - 8080:8080
    #   - 8081:8081
    #   - 8082:8082
    network_mode: host
    environment: 
      MICRO_NAMESPACE: "abjnet"
      MICRO_REGISTERY: "mdns"